# 2026-08-15 两分支合并说明

## 主干选择

以包含持久化 `LearnerModelService`、自适应诊断 API、信息增益选题、120 题题库、工作流诊断暂停/恢复、HarmonyOS 诊断类型接口的分支作为主干。

## 从另一分支保留/合入

1. 保留 `backend/app/services/bkt_service.py`：提供难度校准的 BKT、Beta 辅助、置信度和信息增益纯函数。
2. 保留 `backend/tests/test_diagnosis_service.py`：覆盖 BKT 数学性质。
3. 固定测评 `POST /assessments/{id}/submit` 改为 BKT 后验更新，不再只使用固定 0.65/0.15/0.20 加权分数。
4. 固定测评结果补回 `misconceptions` 聚合，并保留 `evidence_count`。
5. 其余冲突文件采用自适应诊断分支版本，因为它是功能超集：包含 LearnerModel、动态停止、诊断会话持久化、Planner 后验驱动、Retriever 引用证据 pin、数据库兼容迁移和 HarmonyOS Diagnosis API。

## 未机械合入的旧实现

旧版 491 行 `DiagnosisAgent` 没有直接覆盖新版。其目标技能/先修链思想已由新版 `InformationGainDiagnosisService.prerequisite_skills()` 与 `PlannerAgent` 后验驱动路径吸收；直接覆盖会破坏持久化 LearnerModel 与动态诊断工作流。
