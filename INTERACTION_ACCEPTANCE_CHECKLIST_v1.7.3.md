# v1.7.3 运行交互验收清单

## 1. 诊断页

1. 从首页选择 ROS2 或 C 语言诊断。
2. 点击任一答案的文字区域：Radio、圆形字母、背景、边框和勾选应立即变化。
3. 改选另一答案：旧答案取消高亮，新答案高亮。
4. 点击下一题，再返回：原答案保持高亮。
5. 未答完时点击“提交诊断”：应定位第一道未答题并显示剩余题数。
6. 完成全部题目后提交，进入答案回看。
7. 确认后启动真实后端工作流并进入工作流追踪页。

## 2. 画像页

1. 学历、讲解风格、资源偏好分别改选。
2. 每组只能有一个 Radio 被选中。
3. 未同意隐私时保存按钮不可用。
4. 点击隐私卡后显示“已同意”，保存按钮可用。
5. 保存后应通过配置的 API 创建或更新学习者。

## 3. 工作流页

1. 工作流状态通过后端 `/api/v1/workflows/*` 更新。
2. Risk Router、Generation、ProofGraph、Review、Critic/Judge 等步骤按实际 trace 显示。
3. Fast 路径未启用的 Critic/Judge 应显示 skipped，而不是伪造执行成功。
4. 后端不可用时只允许显示缓存快照和连接错误，不得产生本地模拟工作流。
5. 工作流失败后“重新执行工作流”应创建新工作流并重新追踪。

## 4. 资源页

1. 实操每步选择成功或失败，对应按钮保持高亮。
2. 测试题点击文字区域也能选中。
3. 未答完点击提交测试，显示剩余题数。
4. 全部作答后提交，显示得分、正确答案和解析。
5. 未完成测试或实操时点击反馈，应自动切换到相应标签并提示。
6. Evidence、ProofGraph 风险路由和验证结果来自真实后端资源数据。

## 5. API 与单运行链路

1. API 设置页保存后端 HTTP 地址。
2. HarmonyOS 模拟器默认使用 `http://10.0.2.2:8000`。
3. 应用代码中不得存在 `DemoBackend`、`DemoCenterPage`、`demoMode` 或内置模拟数据回退。
4. 后端连接失败时应提示错误，不得自动切换另一套运行时。
5. 清除本地数据失败时必须弹出错误信息。
